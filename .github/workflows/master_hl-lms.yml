# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - HL-LMS

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read #This is required for actions/checkout

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to container registry
        uses: docker/login-action@v2
        with:
          registry: hllmsacr.azurecr.io/
          username: ${{ secrets.AZUREAPPSERVICE_CONTAINERUSERNAME_16ABC203A7644A42A41259F3E8BC87B1 }}
          password: ${{ secrets.AZUREAPPSERVICE_CONTAINERPASSWORD_A4E8B33D4126407BAC76755BED9EF039 }}

      - name: Build and push container image to registry
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true
          tags: hllmsacr.azurecr.io/${{ secrets.AZUREAPPSERVICE_CONTAINERUSERNAME_16ABC203A7644A42A41259F3E8BC87B1 }}/myapp:${{ github.sha }}
          file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    
    needs: build
    
    steps:
      
      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'HL-LMS'
          slot-name: 'Production'
          images: 'hllmsacr.azurecr.io/${{ secrets.AZUREAPPSERVICE_CONTAINERUSERNAME_16ABC203A7644A42A41259F3E8BC87B1 }}/myapp:${{ github.sha }}'
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_691001A9FEAD4CAEAF206A848AB6FD60 }}